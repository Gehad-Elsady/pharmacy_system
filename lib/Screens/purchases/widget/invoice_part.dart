import 'package:flutter/material.dart';

class InvoicePart extends StatelessWidget {
  const InvoicePart({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
